{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "prisma generate --schema=../../packages/db/prisma/schema.prisma && next build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
